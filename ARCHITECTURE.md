# 工业机械核心部件故障诊断与维护助手

## 项目概述

本项目旨在为工业机械核心部件（轴承、电机等）提供智能故障诊断与维护建议。系统支持两种使用模式：
- **轻量版**：基于规则引擎 + 向量检索（RAG）的快速诊断
- **进阶版**：增加机器学习模型，基于振动信号进行自动故障分类

目标用户：设备维护工程师、工厂技术人员、相关领域学生

---

## 一、项目架构设计

### 1.1 轻量版架构

```
┌─────────────────────────────────────────────────────────────┐
│                       用户界面层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ Streamlit Web │  │  命令行工具   │  │  API 接口    │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                       应用逻辑层                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           故障诊断引擎 (Diagnosis Engine)              │  │
│  │  ┌────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │  规则匹配  │  │  向量检索   │  │ 结果融合  │ │  │
│  │  └────────────┘  └─────────────┘  └───────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │            维护建议生成器 (Advice Generator)           │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                       数据处理层                              │
│  ┌────────────────┐  ┌─────────────────┐  ┌───────────┐ │
│  │  知识库 (JSON) │  │  向量数据库     │  │ 规则库    │ │
│  │                │  │  (Chroma/Faiss)│  │ (YAML)   │ │
│  └────────────────┘  └─────────────────┘  └───────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 轻量版数据流

```
用户输入故障现象
    ↓
[文本预处理] → 分词、去停用词
    ↓
┌─────────────────┐
│  并行处理       │
├─────────────────┤
│ 1. 规则引擎匹配 │ → 关键词/模式匹配 → 可能故障列表
│ 2. 向量检索RAG  │ → 查询向量化 → 相似度检索 → 相关知识片段
└─────────────────┘
    ↓
[结果融合] → 综合规则匹配和向量检索结果
    ↓
[生成维护建议] → 格式化输出（故障原因、解决方案、预防措施）
    ↓
返回给用户
```

---

### 1.2 进阶版架构

```
┌─────────────────────────────────────────────────────────────┐
│                       用户界面层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ Streamlit Web │  │  命令行工具   │  │  API 接口    │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                       应用逻辑层                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           故障诊断引擎 (Diagnosis Engine)              │  │
│  │  ┌────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │  规则匹配  │  │  向量检索   │  │ 模型预测  │ │  │
│  │  └────────────┘  └─────────────┘  └───────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │          signal Processing Module                     │  │
│  │  ┌────────────┐  ┌─────────────┐  ┌───────────┐ │  │
│  │  │ 信号加载   │  │ 特征提取    │  │ 降维/选择 │ │  │
│  │  └────────────┘  └─────────────┘  └───────────┘ │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                       模型层                                 │
│  ┌────────────────┐  ┌─────────────────┐  ┌───────────┐ │
│  │  训练好的模型  │  │  特征 scaler   │  │  编码器   │ │
│  │  (Sklearn/PyTorch) │  │                 │  │           │ │
│  └────────────────┘  └─────────────────┘  └───────────┘ │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                       数据存储层                              │
│  ┌────────────────┐  ┌─────────────────┐  ┌───────────┐ │
│  │  知识库 (JSON) │  │  向量数据库     │  │ 规则库    │ │
│  │  训练数据集    │  │  (Chroma/Faiss)│  │ (YAML)   │ │
│  │  CWRU数据集    │  │                 │  │           │ │
│  └────────────────┘  └─────────────────┘  └───────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 进阶版数据流（增加信号分析）

```
用户输入故障现象 或 上传振动信号文件
    ↓
┌─────────────────────────────────────────┐
│  判断输入类型                          │
├─────────────────────────────────────────┤
│  文本描述 → 走轻量版流程               │
│  信号文件 → 走信号分析流程             │
└─────────────────────────────────────────┘
    ↓ (如果是信号文件)
[信号加载] → 读取.mat/.csv文件
    ↓
[预处理] → 去均值、滤波、分段
    ↓
[特征提取] → 时域特征、频域特征、时频特征
    ↓
[机器学习模型预测] → 输出故障类型和概率
    ↓
[查询知识库] → 获取该故障的详细信息
    ↓
[生成维护建议] → 结合模型预测和知识库
    ↓
返回给用户
```

---

## 二、技术选型清单

### 2.1 轻量版技术栈

| 类别 | 技术/工具 | 版本建议 | 用途说明 |
|------|----------|---------|---------|
| **编程语言** | Python | 3.9+ | 主开发语言 |
| **Web框架** | Streamlit | 1.28+ | 快速构建交互式界面 |
| **向量数据库** | ChromaDB | 0.4.0+ | 轻量级向量存储，支持持久化 |
| **向量嵌入** | Sentence Transformers | 2.2.2+ | 文本向量化，中文支持 |
| **规则引擎** | YAML + Python | - | 规则定义和解析 |
| **中文分词** | Jieba | 0.42.1+ | 中文文本预处理 |
| **数据处理** | Pandas | 1.5.0+ | 数据加载和处理 |
| **配置文件** | PyYAML | 6.0+ | 读取规则配置 |
| **日志** | Logging (标准库) | - | 日志记录 |

#### 轻量版依赖包 (requirements_lite.txt)

```txt
streamlit>=1.28.0
chromadb>=0.4.0
sentence-transformers>=2.2.2
jieba>=0.42.1
pandas>=1.5.0
PyYAML>=6.0
numpy>=1.24.0
```

---

### 2.2 进阶版技术栈（在轻量版基础上增加）

| 类别 | 技术/工具 | 版本建议 | 用途说明 |
|------|----------|---------|---------|
| **机器学习** | Scikit-learn | 1.3.0+ | 分类模型训练与预测 |
| **深度学习** | PyTorch | 2.0.0+ | 可选，用于深度学习模型 |
| **信号处理** | SciPy | 1.10.0+ | 信号处理和滤波 |
| **特征提取** | NumPy, SciPy | - | 时域/频域特征计算 |
| **模型持久化** | Joblib | 1.3.0+ | 模型保存和加载 |
| **数据集** | CWRU轴承数据集 | - | 公开轴承故障数据 |
| **可视化** | Matplotlib | 3.6.0+ | 信号和特征可视化 |
| **进阶向量模型** | Modelscope扽Echo | 可选 | 中文更好的嵌入模型 |

#### 进阶版依赖包 (requirements_full.txt)

```txt
# 轻量版依赖
streamlit>=1.28.0
chromadb>=0.4.0
sentence-transformers>=2.2.2
jieba>=0.42.1
pandas>=1.5.0
PyYAML>=6.0
numpy>=1.24.0

# 进阶版新增
scikit-learn>=1.3.0
torch>=2.0.0
scipy>=1.10.0
joblib>=1.3.0
matplotlib>=3.6.0
```

---

## 三、核心模块说明

### 3.1 知识库模块 (knowledge_base/)

**功能**：存储故障诊断领域知识

**数据格式** (JSON):
```json
{
  "bearing_faults": [
    {
      "id": "BF001",
      "name": "内圈故障",
      "symptoms": ["异常振动", "噪声增大", "温度升高"],
      "causes": ["疲劳磨损", "润滑不良", "安装不当"],
      "solutions": ["检查润滑", "更换轴承", "调整安装"],
      "prevention": ["定期润滑", "避免过载", "正确安装"],
      "severity": "高",
      "related_parts": ["轴承内圈", "滚子"]
    }
  ],
  "motor_faults": [
    {
      "id": "MF001",
      "name": "电机过热",
      "symptoms": ["外壳温度高", "电流增大", "效率下降"],
      "causes": ["过载", "散热不良", "绕组短路"],
      "solutions": ["降低负载", "清理散热片", "检修绕组"],
      "prevention": ["定期维护", "监控温度", "避免过载"],
      "severity": "中",
      "related_parts": ["定子绕组", "散热风扇"]
    }
  ]
}
```

### 3.2 规则引擎模块 (rules_engine/)

**功能**：基于专家规则进行故障诊断

**规则格式** (YAML):
```yaml
rules:
  - name: "轴承内圈故障诊断"
    conditions:
      - type: "keyword"
        field: "symptoms"
        keywords: ["振动", "噪声", "温度"]
        match_type: "any"  # any或all
    conclusion:
      fault_id: "BF001"
      confidence: 0.8
      message: "检测到轴承内圈故障可能性"
    
  - name: "电机过热诊断"
    conditions:
      - type: "keyword"
        field: "symptoms"
        keywords: ["温度", "过热", "电流"]
        match_type: "any"
    conclusion:
      fault_id: "MF001"
      confidence: 0.85
      message: "检测到电机过热迹象"
```

### 3.3 RAG向量检索模块 (rag_module/)

**功能**：基于向量相似度的知识检索

**工作流程**:
1. 加载知识库文档，分块
2. 使用Sentence Transformers将文本转为向量
3. 存储到ChromaDB
4. 用户查询时，将查询转为向量，检索最相似的Top-K个文档
5. 将检索结果作为上下文，生成答案

### 3.4 Streamlit前端模块 (streamlit_app/)

**功能**：提供Web交互界面

**界面设计**:
- 主页面：故障描述输入框
- 侧边栏：诊断模式选择（规则/向量/混合）
- 结果页面：故障类型、原因、解决方案、预防措施
- 进阶功能：信号上传和可视化

---

## 四、项目目录结构

```
industrial-fault-diagnosis/
├── README.md                    # 项目说明
├── ARCHITECTURE.md             # 本文件，架构设计
├── requirements_lite.txt       # 轻量版依赖
├── requirements_full.txt        # 进阶版依赖
├── setup.py                    # 安装脚本
├── run.py                      # 启动脚本
│
├── knowledge_base/             # 知识库模块
│   ├── __init__.py
│   ├── fault_data.json        # 故障知识数据
│   ├── loader.py             # 数据加载器
│   └── data_structure.py     # 数据结构定义
│
├── rules_engine/              # 规则引擎模块
│   ├── __init__.py
│   ├── rules.yaml            # 规则定义文件
│   ├── engine.py             # 规则引擎实现
│   └── matcher.py            # 规则匹配器
│
├── rag_module/               # RAG向量检索模块
│   ├── __init__.py
│   ├── vector_store.py      # 向量数据库接口
│   ├── embedding.py         # 文本向量化
│   └── retriever.py         # 检索器
│
├── diagnosis_engine/         # 诊断引擎（核心）
│   ├── __init__.py
│   ├── engine.py            # 主诊断引擎
│   └── advisor.py           # 维护建议生成器
│
├── streamlit_app/            # Streamlit前端
│   ├── __init__.py
│   ├── app.py               # 主应用
│   ├── pages/               # 多页面
│   │   ├── home.py
│   │   ├── diagnosis.py
│   │   └── knowledge.py
│   └── components/          # UI组件
│       ├── input_form.py
│       └── result_display.py
│
├── models/                   # 进阶版：机器学习模型
│   ├── __init__.py
│   ├── trainer.py           # 模型训练
│   ├── predictor.py         # 模型预测
│   └── features.py         # 特征工程
│
├── data/                     # 数据目录
│   ├── raw/                 # 原始数据（如CWRU数据集）
│   └── processed/           # 处理后的数据
│
├── tests/                    # 测试
│   ├── test_rules_engine.py
│   ├── test_rag_module.py
│   └── test_diagnosis_engine.py
│
└── docs/                     # 文档
    ├── API.md               # API文档
    └── DEPLOYMENT.md       # 部署文档
```

---

## 五、后续优化方向

1. **多模态输入**：支持上传图片（设备照片）、音频（异常声音）
2. **知识图谱**：构建故障-原因-解决方案的知识图谱
3. **实时监测接口**：接入IoT设备数据，实时诊断
4. **模型持续优化**：收集用户反馈，持续训练模型
5. **多语言支持**：支持英文、西班牙文等

---

## 六、面试亮点

1. **完整AI应用链路**：从知识库构建 → 规则引擎 → 向量检索 → 前端展示
2. **实际工业场景**：解决真实痛点（设备故障诊断）
3. **可扩展性设计**：轻量版 → 进阶版，模块化设计
4. **技术栈全面**：NLP（向量检索）+ 机器学习 + Web应用
5. **数据驱动**：可以展示从数据到模型的全过程

---

*创建时间: 2026-05-17*
*版本: v1.0*
